{
  "name": "meraki-santier",
  "version": "1.0.0",
  "description": "Checklist de șantier pentru Meraki Lago Resort & Spa",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "kv:create": "wrangler kv namespace create STATE",
    "logs": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
